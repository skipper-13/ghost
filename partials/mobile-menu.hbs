{{!-- Menu --}}
<div class="fixed shadow-xl z-[100] top-[64px] w-full bg-bgr text-typ-tone py-4 border-t border-brd hidden md:!hidden" 
  :class="menuOpen ? '!block' : 'hidden'" data-menu id="menu"
> 
  <div class="px-5 sm:px-6">
    <nav class="" data-nav="mobile">
      {{navigation 
        ul_class="flex flex-col gap-1"
        li_class="flex flex-wrap relative"
        a_class="flex py-0.5 rounded-theme hover:text-brand"
      }}
    </nav>
  </div>
</div>